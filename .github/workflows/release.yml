name: Release Pipeline

# Wird ausgelöst, wenn ein Versions-Tag gepusht wird (z.B. durch den release_version Skill)
on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    # SETUP: Definiere hier das Release-Deployment für das Projekt
    steps:
    - uses: actions/checkout@v4
    
    # 1. Setup und Build
    # - name: Use Node.js
    #   uses: actions/setup-node@v4
    #   with:
    #     node-version: '20.x'
    # - run: npm ci
    # - run: npm run build
    
    # 2. Release via GitHub erstellen
    # Dies verknüpft das publizierte Artefakt mit dem Release-Tag.
    # Alternativ kann Antigravity das auch via mcp_github-mcp-server_create_release tun.
    # - name: Create GitHub Release
    #   uses: softprops/action-gh-release@v1
    #   with:
    #     generate_release_notes: true
    #     files: |
    #       dist/*
